# Postgres connection string (Neon, Supabase, or any Postgres).
# Neon: copy the "Pooled connection" string from the dashboard.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Optional: secret used by the /api/cron/interest endpoint so only Vercel Cron can call it.
CRON_SECRET=""
